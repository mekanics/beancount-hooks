# beancount-hooks

Rule-based Beangulp import hooks for Beancount v3. Predicts missing posting accounts, payees, and tags on imported transactions using a rule-based decision ladder — no ML, no external runtime dependencies beyond Beancount.
